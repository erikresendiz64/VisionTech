import cv2
import time
import mediapipe as mp
import FDmodule

FDmodule.main()
# FD = FDmodule.FaceDetector(0.75).main
# cam = cv2.VideoCapture(0)
# pTime = 0
# while True:
#     ret, frame = cam.read() #read each frame, return true if a frame exists
#     frame, bounds = FD.findFaces(frame)

#     cv2.imshow("Recording", frame)
#     cv2.waitKey(1)


